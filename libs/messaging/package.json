{
  "name": "@app/messaging",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "NATS + JetStream plumbing: connection, stream bootstrap, publisher, durable consumer.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@app/contracts": "^0.1.0",
    "nats": "^2.29.0",
    "pino": "^9.4.0",
    "zod": "^3.25.76"
  }
}
