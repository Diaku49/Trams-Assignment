{
  "name": "@app/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Shared event, DTO and subject definitions used by all three services.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
